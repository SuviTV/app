{
  "name": "suvi",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^4.0.1",
    "massive": "^5.5.0",
    "nodemon": "^1.18.4",
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-scripts": "1.1.5"
  },
  "scripts": {
    "front:start": "react-scripts start",
    "front:build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "back:start": "nodemon server",
    "start": "concurrently 'npm run front:start' npm run back:start"
  }
}
